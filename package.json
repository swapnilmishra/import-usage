{
  "name": "import-usage-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "yarn workspace test start",
    "publish-pkg": "yarn workspace import-usage publish"
  }
}
